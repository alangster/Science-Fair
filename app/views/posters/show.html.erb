
<div class="show_poster">
  <!-- Lets make a simple image magnifier -->
  <div class="magnify">

    <!-- This is the magnifying glass which will contain the original/large version -->
    <div class="large">
    </div>

    <!-- This is the small image -->
      <%= image_tag @poster.filepath, width: "90%", height: "90%", class: "small" %>
  </div>

  <!-- Lets load up prefixfree to handle CSS3 vendor prefixes -->
  <script src="http://thecodeplayer.com/uploads/js/prefixfree.js" type="text/javascript"></script>
  <!-- You can download it from http://leaverou.github.com/prefixfree/ -->

  <!-- Time for jquery action -->
  <script src="http://thecodeplayer.com/uploads/js/jquery-1.7.1.min.js" type="text/javascript"></script>



  <div class="comments_bar">
    <h2>Comments</h2>
    <ul>
      <% @poster.comments.each do |comment| %>
        <li><%= comment.text %></li>
        <% if comment.get_all_subcomments != nil %>
          <ul>
            <% comment.get_all_subcomments.each do |c| %>
            <li><a href="/users/<%= c. commentable.user.id %>"><%= c.commentable.user.first_name %> <%= c.commentable.user.last_name %></a><%= c.text %></li>
            <% end %>
          </ul>
        <% end %>
      <% end %>
    </ul>
  </div>

  <div class="poster_info_bar">
  </div>
</div>

