<div class="col-1-2 poster">
  <div class="poster-pic">
  </div>
  <div class="poster-info">
    <p><%= link_to "#{poster.title}", poster %></p>
    <ul>
      <% poster.users.each do |user| %>
      <li><%= link_to "#{user.name}", user %></li>
      <% end %>
    </ul>
  </div>
</div>

<!-- <div class="grid">
  render "multiple_poster" locals {@posters}
</div>
poster -->
